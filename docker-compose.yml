version: "3.8"

networks:
  frontend_network:
  backend_network:
  db_network:


services:

  db:
    image: postgres
    volumes:
      - ./db:/var/lib/postgresql/data
    env_file:
      - ./.env
    networks:
      - db_network

  backend:
    build: ./backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/code
    ports:
      - "8001:8000"
    env_file:
      - ./.env
    depends_on:
      - db
    networks:
      - frontend_network
      - backend_network
      - db_network
